streamlit
opencv-python-headless
pillow
numpy

# --- OCR engines ---
# Tesseract wrapper (lightweight, requires the tesseract binary - see packages.txt)
pytesseract

# EasyOCR (pulls in torch/torchvision - heavier install)
easyocr

# PaddleOCR (heavier install, Python <=3.13 required - see README)
paddlepaddle
paddleocr

# DocTR (pulls in torch or tensorflow - heavier install)
python-doctr[torch]